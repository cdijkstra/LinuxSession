1. Append aliases to ~/.bashrc and source that file. Try using them out.
2. Create an alias named l that runs the ls command with the -lh flags, providing a long list format with human-readable file sizes.
3. Create an alias that does two things in a row; dnf update and dnf list installed.

