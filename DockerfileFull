# Use the "tianon/full" Docker image as the base image
FROM tianon/debian-devel

# Set a working directory in the container (optional)
WORKDIR /app

COPY Assignments /app/Assignments

# Start a shell inside the container
CMD ["/bin/bash"]

