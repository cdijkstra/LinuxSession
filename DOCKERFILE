# Use a base image (e.g., Ubuntu)
FROM ubuntu:latest

# Update package lists and install any required packages
RUN apt-get update

# Copy the assignments directory into the container
COPY ./assignments /assignments

# Set the working directory
WORKDIR /assignments
